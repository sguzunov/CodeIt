version: 1.0.{build}
branches:
  only:
  - master
before_build:
- cmd: nuget restore CodeIt/CodeIt.sln
build:
  project: CodeIt/CodeIt.sln
  publish_nuget: true
  include_nuget_references: true
  verbosity: minimal